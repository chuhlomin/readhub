readhub
=======

Installation
------------

    pip install Flask
    
    sudo npm install
    sudo npm install -g grunt-cli
    ./node_modules/.bin/bower install
    grunt
    
    python readhub.py
